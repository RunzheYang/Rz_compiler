int main() {
	int a;
	int b = 1 - 1 + 3 * 4;
	int c = a = (1 - b - 1) * (b / 2) % (2 + 3 + b) / b;
	a = 2 >> (c << a) + true;

	bool is_right;
	is_right = false && ((1 < b) != true) && true || true && ((1 < b) != true) && true;

	return 0;
}